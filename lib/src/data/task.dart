import 'dart:ui';

class Task {
  String? name;
  Color? color;
  DateTime? time;
  String? place;
  String? level;
  bool? isDone;
  Task(this.name, this.color, this.time, this.place, this.level, this.isDone);
}
